(function executeRule(current, previous /*null when async*/) {
    var comments;
    current.u_comments = previous.u_action_plan.toString();
        current.u_comments = comments;
        comments + "Plano de ação";
        current.update();
    })(current, previous);